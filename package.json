{
  "private": true,
  "workspaces": [
    "csp-backend",
    "csp-frontend"
  ],
  "scripts": {
    "build": "npm run build --workspace=csp-frontend && mkdir -p public && cp -rv csp-frontend/dist/. public/",
    "postinstall": "npx prisma@6.19.1 generate --schema=csp-backend/prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "6.19.1",
    "prisma": "6.19.1"
  },
  "devDependencies": {
    "@vercel/node": "^5.5.27"
  }
}