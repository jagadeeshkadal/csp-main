{
  "private": true,
  "workspaces": [
    "csp-backend",
    "csp-frontend"
  ],
  "scripts": {
    "build": "npm run build --workspace=csp-frontend && cp -r csp-frontend/dist ./dist",
    "vercel-build": "npm --prefix csp-frontend install --include=dev && npm --prefix csp-frontend run build && cp -r csp-frontend/dist ./dist",
    "postinstall": "npx prisma@6.19.1 generate --schema=csp-backend/prisma/schema.prisma",
    "test": "echo \"No tests specified\""
  },
  "dependencies": {
    "@prisma/client": "6.19.1",
    "prisma": "6.19.1"
  },
  "devDependencies": {
    "@vercel/node": "^5.5.27"
  }
}